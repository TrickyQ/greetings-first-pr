# Greetings

A tiny practice project for shipping a first pull request.

## What it does

It provides a single `greet()` function that returns a friendly greeting.

## Usage

```python
from greetings import greet

print(greet("Sam"))  # Hello, Sam!
```

## Runing the tests

```bash
python -m pytest
```
